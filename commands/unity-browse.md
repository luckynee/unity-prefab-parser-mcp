---
description: Browse Unity project folder tree with asset counts
agent: orchestrator
---

Load the `unity-asset-workflow` skill, then call `browse_unity_project` with the arguments: $ARGUMENTS.

Show the folder tree with asset counts per folder so I can find the right subfolder to work in.

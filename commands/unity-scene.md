---
description: Get a token-efficient overview of a Unity scene
agent: orchestrator
---

Load the `unity-scene-workflow` skill, then parse the Unity scene at "$ARGUMENTS" with preset: "minimal" to get a token-efficient hierarchy overview.

After showing the overview, ask what area or component type to drill into next.
